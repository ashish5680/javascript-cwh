console.log("Welcome to tutorial number 23");
// Math Object in JavaScript

console.log('\n');

let x = 3;
let y = 6;
let z;
z = x+y;
z = x-y;
z = x*y;
z = x/y;




// Exploring the Math object
z = Math;
z = Math.PI;
z = Math.E;       // oilers Number
z = Math.round(5.4);
z = Math.ceil(5.3);
z = Math.floor(5.3);
z = Math.floor(-5.3);      //  -> iska output 6 aayega kuki ye -ve mai hai
z = Math.abs(5);
z = Math.sqrt(64);
z = Math.pow(2, 3);
z = Math.min(2, 3, 34,234, 2342,34);
z = Math.max(2, 3, 34,234,34);
  


z = Math.random();     // for number lie between 0 to 1 including decimal value
z = 100*Math.random();         // for number lie between 0 to 100 including decimal value
z = Math.ceil(10 + (100-10)*Math.random());   // for number lie between 10 to 100 only for integer value
z = Math.ceil(50 + (100-50)*Math.random());   // for number lie between 50 to 100 only for integer value


// For Understang of Random Numbers (Lie in Range)
// a = (0, 1)      // (for number lie between 0 to 1)
// a100 = a*100 = (0, 100)       // (for number lie between 0 to 100)
// a10_100 = 10+a*(100 - 10)     // (for number lie between 10 to 100)
// a10_100 = 50+a*(100 - 50)     // (for number lie between 50 to 100)



console.log(z);








// For Understang of Floor and Ceiling Value
// 3
// 2.8
// 2
// 1
// 0
// -1
// -2
// -2.7
// -3
 